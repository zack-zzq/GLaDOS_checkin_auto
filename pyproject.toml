[project]
name = "glados-checkin-auto"
version = "1.0.0"
description = "GLaDOS 自动签到工具，支持 Docker 部署和 Gotify 消息推送"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "schedule>=1.2.0",
]
